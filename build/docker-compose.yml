version: '3'

services:
  websvr:
    # A container that exposes an API to show its IP address
    image: nginx
    labels:
      - "traefik.http.routers.websvr.rule=Host(`sunsea.ru`)"
      
    restart: always
    volumes:
      - /site/hugo:/site
     
